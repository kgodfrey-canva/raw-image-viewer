# VS Code 扩展发布包 - 最小化白名单
# 只包含扩展运行必需的文件

# 忽略所有文件
**

# === 核心运行文件 ===
!package.json
!README.md
!LICENSE

# === 编译后的代码（必需） ===
!out/extension/**
!out/shared/**

# === 构建后的webview（必需） ===
!dist/webview/**
